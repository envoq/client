import { Activity, ArrowUpRight, Bot, Inbox, RefreshCw, Server, Wifi } from 'lucide-react'
import { getBrokerStatus } from '@/lib/broker'
const activity = [{ name: 'agent.support', action: 'sent a message to', target: 'agent.billing', time: '2m ago', status: 'Delivered' }, { name: 'agent.ops', action: 'discovered', target: 'agent.inventory', time: '11m ago', status: 'Connected' }, { name: 'agent.billing', action: 'completed transfer', target: 'invoice_1042.pdf', time: '28m ago', status: 'Verified' }]
export default async function Overview() { const broker = await getBrokerStatus(); return <div><div className="flex flex-col justify-between gap-5 md:flex-row md:items-end"><div><p className="font-mono text-xs uppercase tracking-[.2em] text-primary">Wednesday, August 27</p><h1 className="mt-2 text-4xl font-semibold tracking-[-.05em]">Good morning, Alex.</h1><p className="mt-2 text-muted-foreground">Here&apos;s what&apos;s happening across your agent network.</p></div><button className="flex w-fit items-center gap-2 rounded-lg border border-border bg-card px-3 py-2 text-sm"><RefreshCw data-icon="inline-start" /> Refresh</button></div><div className="mt-10 grid gap-4 sm:grid-cols-2 lg:grid-cols-4">{[{ icon: Server, label: 'Broker status', value: broker.connected ? 'Operational' : 'Configuration needed', detail: broker.connected ? 'Healthy' : 'Action needed' }, { icon: Bot, label: 'Active agents', value: broker.agents ? String(broker.agents) : '—', detail: 'Across 3 namespaces' }, { icon: Inbox, label: 'Messages today', value: broker.messages ? String(broker.messages) : '—', detail: 'Awaiting live data' }, { icon: Wifi, label: 'Avg. latency', value: broker.latency ? `${broker.latency}ms` : '—', detail: 'Last 15 minutes' }].map(({ icon: Icon, label, value, detail }) => <div key={String(label)} className="rounded-xl border border-border bg-card p-5"><div className="flex items-center justify-between text-muted-foreground"><span className="text-sm">{String(label)}</span><Icon className="text-primary" data-icon="inline-start" /></div><p className="mt-5 text-2xl font-semibold">{String(value)}</p><p className="mt-1 text-xs text-muted-foreground">{String(detail)}</p></div>)}</div><div className="mt-8 grid gap-6 lg:grid-cols-[1.3fr_.7fr]"><section className="rounded-xl border border-border bg-card"><div className="flex items-center justify-between border-b border-border p-5"><div><h2 className="font-semibold">Recent activity</h2><p className="mt-1 text-sm text-muted-foreground">Your network in motion.</p></div><Activity className="text-primary" data-icon="inline-start" /></div><div className="flex flex-col">{activity.map(item => <div key={item.name + item.time} className="flex gap-4 border-b border-border p-5 last:border-0"><span className="mt-1 size-2 rounded-full bg-accent-foreground" /><div className="text-sm"><p><strong>{item.name}</strong> {item.action} <strong>{item.target}</strong></p><p className="mt-1 text-xs text-muted-foreground">{item.time} · {item.status}</p></div></div>)}</div></section><section className="rounded-xl bg-primary p-6 text-primary-foreground"><p className="font-mono text-xs uppercase tracking-[.2em] text-primary-foreground/70">Get connected</p><h2 className="mt-12 text-2xl font-semibold tracking-[-.03em]">Add your first agent to the network.</h2><p className="mt-3 text-sm leading-6 text-primary-foreground/75">Use the SDK or sidecar to register an identity and start exchanging messages.</p><button className="mt-7 flex items-center gap-2 rounded-lg bg-primary-foreground px-4 py-2.5 text-sm font-medium text-primary">View quickstart <ArrowUpRight data-icon="inline-end" /></button></section></div></div> }
