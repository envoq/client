import { KeyRound, Save, ShieldCheck } from 'lucide-react'
export default function Settings() { return <div className="max-w-3xl"><p className="font-mono text-xs uppercase tracking-[.2em] text-primary">Workspace</p><h1 className="mt-2 text-4xl font-semibold tracking-[-.05em]">Settings</h1><p className="mt-2 text-muted-foreground">Manage your broker connection and workspace access.</p><div className="mt-8 flex flex-col gap-5"><section className="rounded-xl border border-border bg-card p-6"><div className="flex items-start gap-4"><span className="grid size-9 place-items-center rounded-lg bg-muted"><KeyRound className="text-primary" data-icon="inline-start" /></span><div><h2 className="font-semibold">Broker connection</h2><p className="mt-1 text-sm text-muted-foreground">Credentials stay server-side and are never exposed to the browser.</p></div></div><label className="mt-6 flex flex-col gap-2 text-sm font-medium">Broker URL<input className="rounded-lg border border-border bg-background px-3 py-2.5 font-mono text-sm outline-none focus:border-primary" placeholder="https://broker.example.com" /></label><button className="mt-5 flex items-center gap-2 rounded-lg bg-primary px-4 py-2.5 text-sm font-medium text-primary-foreground"><Save data-icon="inline-start" /> Save connection</button></section><section className="rounded-xl border border-border bg-card p-6"><div className="flex items-center gap-3"><ShieldCheck className="text-accent-foreground" data-icon="inline-start" /><h2 className="font-semibold">Security</h2></div><p className="mt-3 text-sm leading-6 text-muted-foreground">Agent identities use Ed25519 signatures. Configure policies in your broker to control discovery and transfers.</p></section></div></div> }
