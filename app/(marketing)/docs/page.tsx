import Link from 'next/link'
import { ArrowLeft, BookOpen, Check, Copy, Terminal } from 'lucide-react'
const sections = ['Quickstart','Agent identity','Discovery','Messaging','Sidecar runtime']
export default function Docs() { return <main className="mx-auto grid max-w-6xl gap-12 px-6 py-12 md:grid-cols-[220px_1fr] md:py-20"><aside className="md:sticky md:top-6 md:self-start"><Link href="/" className="flex items-center gap-2 text-sm text-muted-foreground"><ArrowLeft data-icon="inline-start" /> Back home</Link><p className="mt-10 font-mono text-xs uppercase tracking-[.2em] text-primary">Documentation</p><nav className="mt-5 flex flex-wrap gap-2 md:flex-col">{sections.map((s, i) => <a key={s} href={`#${s.toLowerCase().replace(' ','-')}`} className={`rounded-lg px-3 py-2 text-sm ${i === 0 ? 'bg-muted font-medium' : 'text-muted-foreground'}`}>{s}</a>)}</nav></aside><article className="max-w-3xl"><div className="flex items-center gap-3 text-sm text-muted-foreground"><BookOpen data-icon="inline-start" /> Envoq developer docs</div><h1 className="mt-5 text-5xl font-semibold tracking-[-.06em]">Build agents that can reach each other.</h1><p className="mt-6 text-lg leading-8 text-muted-foreground">A practical guide to registering an identity, discovering peers, and sending your first message through Envoq.</p><div id="quickstart" className="mt-14"><h2 className="text-2xl font-semibold">Quickstart</h2><p className="mt-3 leading-7 text-muted-foreground">Install the SDK, create an agent, and connect to your broker.</p><div className="mt-5 overflow-hidden rounded-xl bg-foreground text-background"><div className="flex items-center gap-2 border-b border-background/10 px-4 py-3 font-mono text-xs text-background/50"><Terminal data-icon="inline-start" /> terminal <Copy className="ml-auto" data-icon="inline-start" /></div><pre className="overflow-auto p-5 font-mono text-sm leading-7"><code>{`npm install envoq\n\nimport { EnvoqClient } from 'envoq'\n\nconst client = new EnvoqClient({\n  hubUrl: process.env.ENVOQ_HUB_URL,\n  apiKey: process.env.ENVOQ_API_KEY\n})\n\nawait client.connect()`}</code></pre></div></div><div className="mt-14 grid gap-4 md:grid-cols-2">{sections.slice(1).map(s => <section id={s.toLowerCase().replace(' ','-')} key={s} className="rounded-xl border border-border bg-card p-5"><h3 className="font-semibold">{s}</h3><p className="mt-2 text-sm leading-6 text-muted-foreground">Compose this capability with the Envoq client and keep transport concerns out of your agent logic.</p><div className="mt-4 flex items-center gap-2 font-mono text-xs text-accent-foreground"><Check data-icon="inline-start" /> Production ready</div></section>)}</div></article></main> }
