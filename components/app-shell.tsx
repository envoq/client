import Link from 'next/link'
import { Activity, Bot, ChevronDown, CircleDot, Inbox, LayoutDashboard, Settings, TerminalSquare } from 'lucide-react'

const nav = [{ href: '/overview', label: 'Overview', icon: LayoutDashboard }, { href: '/agents', label: 'Agents', icon: Bot }, { href: '/messages', label: 'Messages', icon: Inbox }, { href: '/settings', label: 'Settings', icon: Settings }]
export function AppShell({ children }: { children: React.ReactNode }) { return <div className="flex min-h-screen bg-background"><aside className="hidden w-64 shrink-0 border-r border-border bg-card px-4 py-5 md:flex md:flex-col"><Link href="/" className="flex items-center gap-2 px-3 font-semibold"><span className="grid size-8 place-items-center rounded-lg bg-primary text-primary-foreground"><TerminalSquare data-icon="inline-start" /></span>envoq</Link><div className="mt-8 flex items-center justify-between rounded-xl bg-muted px-3 py-3 text-sm"><div><p className="font-medium">Acme workspace</p><p className="text-xs text-muted-foreground">Pro plan</p></div><ChevronDown data-icon="inline-end" /></div><nav className="mt-6 flex flex-col gap-1">{nav.map(({ href, label, icon: Icon }) => <Link key={href} href={href} className="flex items-center gap-3 rounded-lg px-3 py-2.5 text-sm text-muted-foreground hover:bg-muted hover:text-foreground"><Icon data-icon="inline-start" />{label}</Link>)}</nav><div className="mt-auto rounded-xl border border-border p-3"><div className="flex items-center gap-2 text-xs font-medium"><CircleDot className="text-accent-foreground" data-icon="inline-start" /> Broker online</div><p className="mt-2 text-xs text-muted-foreground">Connected to production</p></div></aside><main className="min-w-0 flex-1"><header className="flex items-center justify-between border-b border-border bg-card px-6 py-4 md:px-10"><div className="md:hidden"><Link href="/" className="font-semibold">envoq</Link></div><div className="ml-auto flex items-center gap-3 text-sm"><Activity className="text-accent-foreground" data-icon="inline-start" /> <span className="hidden text-muted-foreground sm:inline">All systems operational</span><span className="grid size-8 place-items-center rounded-full bg-primary text-xs font-semibold text-primary-foreground">AS</span></div></header><div className="mx-auto max-w-7xl p-6 md:p-10">{children}</div></main></div> }
